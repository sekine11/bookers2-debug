if (location.pathname == '/books') {
  $(".book_<%= @book.id %>").html("<%= j(render 'books/favorite_index',  book: @book ) %>")
} else {
$(".book_<%= @book.id %>").html("<%= j(render 'books/favorite_show',  book: @book ) %>")
}