<%= form_for [@book, @newcomment], data: {remote: true} do |f| %>
    <%= f.text_area :content, rows:'5', class: "form-control" %>
    <%= f.submit "送信", class: "btn btn-lg btn-base-1 mt-20" %>
<% end %>